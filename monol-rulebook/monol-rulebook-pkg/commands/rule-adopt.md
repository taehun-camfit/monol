---
description: 규칙 채택 (한글: 규칙채택, 규칙가져오기, 규칙설치, 마켓규칙)
argument-hint: "<rule-id> [--customize] [--pin-version]"
allowed-tools: [Read, Write, Edit, Glob, Bash]
---

# /rule-adopt - 규칙 채택

마켓플레이스나 다른 팀의 공개 규칙을 내 팀에 채택합니다.

## 라이브러리 연동

```typescript
import {
  RulebookManager,
  AuthManager,
  getAuthManager,
  RemoteSyncService,
  getRemoteSyncService,
} from './lib';

const auth = getAuthManager();
const syncService = getRemoteSyncService({ authManager: auth });
```

## 사용법

### 1. 마켓플레이스 규칙 채택

```
/rule-adopt security-xss-001
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📥 규칙 채택
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

규칙: security-xss-001
이름: XSS 방지 규칙
제공: Security Team (@security-team)
버전: 2.1.0
채택 수: 1,234

📋 규칙 내용:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
사용자 입력을 HTML에 직접 삽입하지 마세요.
항상 이스케이프 처리하거나 안전한 API를 사용하세요.

✅ Good:
  - element.textContent = userInput;
  - const safe = DOMPurify.sanitize(userInput);

❌ Bad:
  - element.innerHTML = userInput;
  - document.write(userInput);
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

채택하시겠습니까? [Y/N]
```

**채택 완료:**

```
✅ 규칙이 채택되었습니다!

  규칙 ID: security-xss-001
  저장 위치: rules/adopted/security-xss-001.yaml
  연결: 원본 규칙과 연결됨 (업데이트 알림 수신)

💡 규칙 확인: /rule security-xss-001
💡 커스터마이징: /rule-adopt security-xss-001 --customize
💡 동기화: /rule-sync --remote pull
```

### 2. 규칙 검색 후 채택

```
/rule-adopt --search "react hooks"
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 규칙 검색: "react hooks"
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[1] react-hooks-001
    이름: React Hooks 사용 규칙
    제공: React Korea (@react-korea)
    ⭐ 4.8 | 📥 892

[2] react-hooks-deps-001
    이름: useEffect 의존성 규칙
    제공: Frontend Best Practices (@fb-practices)
    ⭐ 4.5 | 📥 567

[3] react-custom-hooks-001
    이름: 커스텀 훅 네이밍 규칙
    제공: Clean Code Team (@clean-code)
    ⭐ 4.3 | 📥 234

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

채택할 규칙 번호를 입력하세요 (여러 개: 1,2,3):
```

### 3. 컬렉션 채택

```
/rule-adopt --collection frontend-essentials
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 컬렉션 채택: Frontend Essentials
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

제공: Frontend Masters (@frontend-masters)
설명: 프론트엔드 개발에 필수적인 규칙 모음
규칙 수: 12개
⭐ 4.7 | 📥 2,345

포함된 규칙:
  1. naming-react-001: React 컴포넌트 네이밍
  2. hooks-deps-001: useEffect 의존성 관리
  3. state-001: 상태 관리 패턴
  4. perf-001: 성능 최적화
  5. a11y-001: 접근성 기본
  ... 7개 더

전체 채택하시겠습니까? [Y/N]
```

### 4. 커스터마이징하여 채택

```
/rule-adopt security-xss-001 --customize
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✏️ 규칙 커스터마이징
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

원본 규칙: security-xss-001

커스터마이징 옵션:
  [1] severity 변경 (현재: error)
  [2] 태그 추가/수정 (현재: security, xss, frontend)
  [3] 예외 조건 추가
  [4] 설명 수정
  [5] 완료

선택:
```

**커스터마이징 후:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ 커스터마이징 완료
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

변경 사항:
  - severity: error → warning
  - tags: +[frontend, react]
  - condition: excludePatterns 추가

저장 위치: rules/adopted/security-xss-001.yaml

⚠️ 커스터마이징된 규칙은 원본 업데이트가 자동 적용되지 않습니다.
   원본 업데이트 알림만 받게 됩니다.
```

### 5. 버전 고정

```
/rule-adopt security-xss-001 --pin-version 2.0.0
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📌 버전 고정 채택
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

규칙: security-xss-001
고정 버전: 2.0.0 (최신: 2.1.0)

⚠️ 버전을 고정하면:
   - 원본 규칙이 업데이트되어도 이 버전이 유지됩니다
   - 보안 업데이트를 놓칠 수 있습니다

계속하시겠습니까? [Y/N]
```

### 6. 채택한 규칙 관리

```
/rule-adopt --list
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📥 채택한 규칙 목록
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  security-xss-001
    버전: 2.1.0 (최신)
    원본: Security Team
    상태: ✅ 연결됨

  react-hooks-001
    버전: 1.5.0 (⚠️ 1.6.0 사용 가능)
    원본: React Korea
    상태: ⚠️ 업데이트 가능

  naming-api-001 [커스터마이징됨]
    버전: 3.0.0
    원본: API Guidelines
    상태: ✏️ 로컬 수정됨

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
총 3개 규칙

💡 업데이트: /rule-adopt --update react-hooks-001
💡 연결 해제: /rule-adopt --unlink naming-api-001
```

### 7. 채택 취소

```
/rule-adopt --remove security-xss-001
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🗑️ 규칙 채택 취소
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

규칙: security-xss-001

삭제할 파일: rules/adopted/security-xss-001.yaml

정말 삭제하시겠습니까? [Y/N]
```

## 옵션

| 옵션 | 설명 |
|------|------|
| `<rule-id>` | 채택할 규칙 ID |
| `--search <query>` | 규칙 검색 |
| `--collection <id>` | 컬렉션 채택 |
| `--customize` | 커스터마이징 모드 |
| `--pin-version <ver>` | 특정 버전 고정 |
| `--list` | 채택한 규칙 목록 |
| `--update [id]` | 채택한 규칙 업데이트 |
| `--remove <id>` | 채택 취소 |
| `--unlink <id>` | 원본 연결 해제 |

## 채택된 규칙 구조

`rules/adopted/security-xss-001.yaml`:

```yaml
id: security-xss-001
name: XSS 방지 규칙
description: |
  사용자 입력을 HTML에 직접 삽입하지 마세요.
category: security/xss
severity: error
tags:
  - security
  - xss
  - frontend

# 채택 메타데이터
origin:
  type: marketplace
  ruleId: security-xss-001
  version: "2.1.0"
  publisherId: security-team
  adoptedAt: "2025-01-22T10:30:00Z"

# 로컬 커스터마이징
customizations:
  severity: warning  # 원본은 error
  additionalTags:
    - react
```

## 구현 로직

```typescript
async function executeRuleAdopt(args: string) {
  const { ruleId, search, collection, customize, pinVersion } = parseArgs(args);

  const auth = getAuthManager();
  if (!auth.isAuthenticated()) {
    console.log('⚠️ 로그인이 필요합니다.');
    return;
  }

  // 검색 모드
  if (search) {
    const results = await searchMarketplace(search);
    const selected = await promptSelection(results);
    ruleId = selected.id;
  }

  // 규칙 조회
  const remoteRule = await fetchMarketplaceRule(ruleId);
  if (!remoteRule) {
    console.log('⚠️ 규칙을 찾을 수 없습니다.');
    return;
  }

  // 커스터마이징
  let finalRule = remoteRule;
  if (customize) {
    finalRule = await promptCustomization(remoteRule);
  }

  // 버전 고정
  if (pinVersion) {
    finalRule.origin.version = pinVersion;
    finalRule.origin.pinned = true;
  }

  // 저장
  const outputPath = path.join('rules', 'adopted', `${ruleId}.yaml`);
  await saveRule(outputPath, finalRule);

  console.log(`✅ 규칙이 채택되었습니다: ${outputPath}`);
}
```

## 업데이트 알림

채택한 규칙의 원본이 업데이트되면 알림을 받습니다:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔔 규칙 업데이트 알림
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

security-xss-001 업데이트 (2.1.0 → 2.2.0)

변경 내용:
  - 새로운 XSS 패턴 추가
  - React 18 지원

💡 업데이트: /rule-adopt --update security-xss-001
💡 변경사항 보기: /rule-history security-xss-001 --remote
```

## 관련 커맨드

- `/rule-publish` - 규칙 발행
- `/rule-team` - 팀 관리
- `/rule-sync --remote` - 팀 규칙 동기화
- `/rule-search` - 규칙 검색
