---
description: 규칙 발행 (한글: 규칙발행, 규칙공개, 규칙배포, 마켓등록)
argument-hint: "<rule-id> [--visibility <team|public>] [--marketplace]"
allowed-tools: [Read, Write, Edit, Glob, Bash]
---

# /rule-publish - 규칙 발행

로컬 규칙을 팀에 공유하거나 마켓플레이스에 발행합니다.

## 라이브러리 연동

```typescript
import {
  RulebookManager,
  AuthManager,
  getAuthManager,
  RemoteSyncService,
  getRemoteSyncService,
} from './lib';

const auth = getAuthManager();
const syncService = getRemoteSyncService({ authManager: auth });
const manager = new RulebookManager(workspacePath);
```

## 사용법

### 1. 팀에 규칙 발행

```
/rule-publish naming-001
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📤 규칙 발행
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

규칙: naming-001 (변수명 규칙)
팀: Design System
공개 범위: team

📋 규칙 미리보기:
   ID: naming-001
   이름: 변수명 규칙
   카테고리: code/naming
   심각도: warning
   태그: naming, variables, style

발행하시겠습니까? [Y/N]
```

**발행 완료:**

```
✅ 규칙이 팀에 발행되었습니다!

  규칙 ID: naming-001
  팀: Design System
  공개 범위: team
  버전: 1.0.0
  발행 시각: 2025-01-22T10:30:00Z

팀원들이 이제 이 규칙을 사용할 수 있습니다.

💡 마켓플레이스 등록: /rule-publish naming-001 --marketplace
💡 전체 규칙 동기화: /rule-sync --remote push
```

### 2. 마켓플레이스에 발행

```
/rule-publish naming-001 --marketplace
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🌐 마켓플레이스 발행
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

규칙: naming-001 (변수명 규칙)

⚠️ 마켓플레이스 발행 시:
   - 모든 사용자가 이 규칙을 볼 수 있습니다
   - 규칙 내용이 공개됩니다
   - 다른 팀에서 채택할 수 있습니다

📋 발행 정보:
   카테고리: Code Style > Naming
   태그: naming, variables, style
   라이선스: MIT

계속하시겠습니까? [Y/N]
```

**발행 완료:**

```
✅ 마켓플레이스에 발행되었습니다!

  규칙 ID: naming-001
  마켓플레이스 URL: https://monol.io/rules/naming-001
  채택 수: 0

💡 규칙 페이지 보기: /rule-market naming-001
💡 통계 확인: /rule-analytics naming-001
```

### 3. 여러 규칙 일괄 발행

```
/rule-publish --all
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📤 규칙 일괄 발행
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

팀: Design System

발행할 규칙 (5개):
  ✓ naming-001: 변수명 규칙
  ✓ style-001: 코드 포맷팅 규칙
  ✓ git-001: 커밋 메시지 규칙
  ○ planning-001: 시나리오 기반 플래닝 (이미 발행됨)
  ○ test-001: 테스트 규칙 (이미 발행됨)

새로 발행할 규칙: 3개
이미 발행됨: 2개

계속하시겠습니까? [Y/N]
```

### 4. 태그로 필터링하여 발행

```
/rule-publish --tag code --tag style
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📤 태그 기반 규칙 발행
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

태그: code, style
일치하는 규칙: 4개

  ✓ naming-001: 변수명 규칙
  ✓ style-001: 코드 포맷팅 규칙
  ✓ format-001: 파일 포맷 규칙
  ✓ lint-001: 린트 설정 규칙

발행하시겠습니까? [Y/N]
```

### 5. 공개 범위 설정

```
/rule-publish naming-001 --visibility public
```

| 범위 | 설명 |
|------|------|
| `private` | 본인만 접근 가능 |
| `team` | 팀 멤버만 접근 가능 (기본값) |
| `public` | 모든 사용자 접근 가능 |

### 6. Dry Run (미리보기)

```
/rule-publish naming-001 --dry-run
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 발행 미리보기 (Dry Run)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

규칙: naming-001

발행될 내용:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{
  "id": "naming-001",
  "name": "변수명 규칙",
  "description": "변수명, 함수명, 클래스명에 대한 네이밍 컨벤션입니다.",
  "category": "code/naming",
  "severity": "warning",
  "tags": ["naming", "variables", "style"],
  "visibility": "team",
  "metadata": {
    "version": "1.0.0",
    "author": "kent"
  }
}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️ Dry run 모드입니다. 실제로 발행되지 않았습니다.
   실제 발행: /rule-publish naming-001
```

## 옵션

| 옵션 | 설명 |
|------|------|
| `<rule-id>` | 발행할 규칙 ID |
| `--all` | 모든 로컬 규칙 발행 |
| `--tag <tag>` | 특정 태그 규칙만 발행 |
| `--category <cat>` | 특정 카테고리만 발행 |
| `--visibility <vis>` | 공개 범위 (private/team/public) |
| `--marketplace` | 마켓플레이스에 등록 |
| `--dry-run` | 미리보기만 |
| `--force` | 확인 없이 발행 |

## 구현 로직

```typescript
async function executeRulePublish(args: string) {
  const { ruleId, all, tag, visibility, marketplace, dryRun } = parseArgs(args);

  const auth = getAuthManager();
  if (!auth.isAuthenticated()) {
    console.log('⚠️ 로그인이 필요합니다.');
    return;
  }

  const manager = new RulebookManager(workspacePath);
  const loaded = await manager.loadRulesForPath(workspacePath);

  // 발행할 규칙 필터링
  let rulesToPublish = loaded.rules;

  if (ruleId) {
    rulesToPublish = rulesToPublish.filter(r => r.id === ruleId);
  }
  if (tag) {
    rulesToPublish = rulesToPublish.filter(r => r.tags?.includes(tag));
  }

  if (rulesToPublish.length === 0) {
    console.log('⚠️ 발행할 규칙이 없습니다.');
    return;
  }

  if (dryRun) {
    displayDryRun(rulesToPublish);
    return;
  }

  // 발행 실행
  const syncService = getRemoteSyncService();

  for (const rule of rulesToPublish) {
    const result = await syncService.pushRule({
      ...rule,
      visibility: visibility || 'team',
      listedInMarketplace: marketplace,
    });

    if (result.success) {
      console.log(`✅ ${rule.id}: 발행 완료`);
    } else {
      console.log(`❌ ${rule.id}: 발행 실패`);
    }
  }
}
```

## 발행 전 체크리스트

규칙 발행 전 자동 검증:

1. **필수 필드 확인**
   - id, name, description 필수
   - category 필수
   - severity 필수

2. **내용 검증**
   - examples.good/bad 권장
   - 설명 최소 길이 확인

3. **민감정보 스캔**
   - API 키, 비밀번호 패턴 검사
   - 내부 URL 검사

```
⚠️ 발행 전 검증 결과:

  ✅ 필수 필드: 모두 있음
  ⚠️ examples.good: 없음 (권장)
  ⚠️ examples.bad: 없음 (권장)
  ❌ 민감정보 발견: line 15에 API_KEY 패턴

계속하시겠습니까? [Y/N]
```

## 관련 커맨드

- `/rule-team` - 팀 관리
- `/rule-adopt` - 규칙 채택
- `/rule-sync --remote` - 팀 규칙 동기화
- `/rule` - 규칙 조회
