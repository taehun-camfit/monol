---
description: 팀 관리 (한글: 팀관리, 팀목록, 팀생성, 팀가입, 팀설정)
argument-hint: "[list | create | join | leave | settings] [options]"
allowed-tools: [Read, Write, Edit, Glob, Bash]
---

# /rule-team - 팀 관리

팀을 생성, 가입, 관리하여 규칙을 공유합니다.

## 라이브러리 연동

```typescript
import {
  AuthManager,
  getAuthManager,
  loadAuthConfigFromEnv,
  RemoteSyncService,
  getRemoteSyncService,
} from './lib';

// 인증 초기화
const authConfig = loadAuthConfigFromEnv();
const auth = getAuthManager(authConfig);
await auth.initialize();

// 원격 서비스 초기화
const syncService = getRemoteSyncService({
  serverUrl: process.env.MONOL_SERVER_URL,
  authManager: auth,
});
```

## 사용법

### 1. 팀 목록 조회

```
/rule-team list
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
👥 내 팀 목록
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🏠 Frontend Team (frontend-team)
   역할: admin
   멤버: 5명
   규칙: 23개

📦 Backend Team (backend-team)
   역할: member
   멤버: 8명
   규칙: 45개

⭐ Design System (design-system) [현재 팀]
   역할: owner
   멤버: 3명
   규칙: 12개

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
총 3개 팀

💡 팀 전환: /rule-team use <team-slug>
💡 팀 생성: /rule-team create <name>
```

### 2. 팀 생성

```
/rule-team create "My New Team"
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎉 팀 생성 완료!
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

팀 이름: My New Team
팀 ID: my-new-team
역할: owner

✅ 현재 팀으로 설정되었습니다.

다음 단계:
  💡 규칙 푸시: /rule-sync --remote push
  💡 멤버 초대: /rule-team invite <email>
  💡 팀 설정: /rule-team settings
```

### 3. 팀 가입

```
/rule-team join frontend-team
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎟️ 팀 가입 요청
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

팀: Frontend Team (frontend-team)
상태: 가입 요청 전송됨

⏳ 팀 관리자의 승인을 기다리고 있습니다.
   승인되면 알림을 받게 됩니다.

💡 초대 코드가 있다면: /rule-team join frontend-team --code ABC123
```

### 4. 팀 전환

```
/rule-team use backend-team
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔄 팀 전환
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

이전 팀: Frontend Team
현재 팀: Backend Team

✅ 팀이 전환되었습니다.

💡 규칙 동기화: /rule-sync --remote pull
```

### 5. 팀 탈퇴

```
/rule-team leave frontend-team
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚠️ 팀 탈퇴 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

팀: Frontend Team

정말 탈퇴하시겠습니까?
- 팀 규칙에 대한 접근 권한이 사라집니다
- 내가 작성한 규칙은 팀에 유지됩니다

[Y/N]
```

### 6. 멤버 초대

```
/rule-team invite user@example.com --role member
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📧 초대 전송 완료
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

팀: Design System
이메일: user@example.com
역할: member

✅ 초대 이메일이 전송되었습니다.
   초대 코드: ABC123 (7일간 유효)

💡 초대 링크 공유: https://monol.io/invite/ABC123
```

### 7. 팀 설정 조회/변경

```
/rule-team settings
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚙️ 팀 설정: Design System
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 승인 설정
   승인 필요: Yes
   최소 승인자 수: 2
   자동 머지: No

🔒 공개 범위
   기본 공개 범위: team (팀 내부만)
   마켓플레이스 발행: No

🔔 알림
   이메일: Yes
   Slack: #design-rules

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 설정 변경: /rule-team settings --set <key>=<value>
예: /rule-team settings --set minApprovers=1
```

### 8. 멤버 목록

```
/rule-team members
```

**출력:**

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
👥 팀 멤버: Design System
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⭐ kent (owner)
   기여: 15개 규칙, 23개 리뷰

👑 alice (admin)
   기여: 8개 규칙, 12개 리뷰

👤 bob (member)
   기여: 3개 규칙, 5개 리뷰

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
총 3명

💡 멤버 초대: /rule-team invite <email>
💡 역할 변경: /rule-team role <username> <role>
```

## 옵션

| 옵션 | 설명 |
|------|------|
| `list` | 팀 목록 조회 |
| `create <name>` | 새 팀 생성 |
| `join <slug>` | 팀 가입 요청 |
| `leave [slug]` | 팀 탈퇴 |
| `use <slug>` | 현재 팀 전환 |
| `invite <email>` | 멤버 초대 |
| `members` | 멤버 목록 |
| `settings` | 팀 설정 조회/변경 |
| `role <user> <role>` | 멤버 역할 변경 |

### 역할 (Role)

| 역할 | 권한 |
|------|------|
| `owner` | 모든 권한 + 팀 삭제 |
| `admin` | 멤버 관리 + 설정 변경 |
| `member` | 규칙 추가/수정/삭제 |
| `viewer` | 규칙 조회만 가능 |

## 구현 로직

```typescript
async function executeRuleTeam(args: string) {
  const [command, ...params] = args.split(/\s+/);

  // 인증 확인
  const auth = getAuthManager();
  if (!auth.isAuthenticated()) {
    console.log('⚠️ 로그인이 필요합니다.');
    console.log('💡 로그인: /rule-auth login');
    return;
  }

  const syncService = getRemoteSyncService();

  switch (command) {
    case 'list':
      const teams = await syncService.getTeams();
      displayTeamList(teams);
      break;

    case 'create':
      const teamName = params.join(' ');
      const newTeam = await createTeam(teamName);
      displayTeamCreated(newTeam);
      break;

    case 'join':
      const slug = params[0];
      await requestJoinTeam(slug);
      break;

    case 'use':
      const targetSlug = params[0];
      syncService.setTeam(targetSlug);
      console.log(`✅ 현재 팀: ${targetSlug}`);
      break;

    case 'members':
      const members = await getTeamMembers();
      displayMembers(members);
      break;

    default:
      displayHelp();
  }
}
```

## 로컬 설정 저장

`~/.config/monol/team.json`:

```json
{
  "currentTeam": "design-system",
  "teams": {
    "design-system": {
      "id": "team_abc123",
      "slug": "design-system",
      "name": "Design System",
      "role": "owner"
    }
  }
}
```

## 관련 커맨드

- `/rule-sync --remote` - 팀 규칙 동기화
- `/rule-publish` - 규칙 발행
- `/rule-adopt` - 규칙 채택
